// Changelog.cfg v1.0.0.1
// Yarbrough
// created: 01 Aug 2018
// updated: 02 Mar 2020

KERBALCHANGELOG //Required to have this name
{
	showChangelog = True //To show the changelog, this must be set to True
	modName = Yarbrough //Add your mod's name here
	VERSION
	{
		version = 0.0.5.0
		change = 
		change = aero cat to Thermal cat
		change = RCS to RCSFX
		change = spelling
		change = formating / code cleaning
		change = crashtolerance 14 from 50
		change = maxtemp from 4000 to 3300
		change = fuelCrossFeed = False
		change = stageOffset = 1
		change = childStageOffset = 1
		change = jettisonedObjectMass = 0.625 from 0.125
		change = jettisonForce = 15 from 2
		change = jettisonDirection = 0 0 1 from 0 0 -1
		change = useChar = True
		change = charModuleName = shieldChar
		change = add
		change = fx_gasBurst_white = 0, 0.0, 0.7, 0, 0.0, -1.0, decouple
		change = sound_vent_large = decouple
		change = tweakscale
		change = shieldCanThrust = false
		change = stagingEnabled = False
		change = runningEffectName = running
		change = EFFECTS: running
		change = AUDIO_MULTI_POOL
		change = MODEL_MULTI_PARTICLE
		change = stagingEnabled = False
		change = checkBottomNode = True
		change = add moduleliftingsurface
		change = adjust ModuleDecouple
		change = added * tags
	}
	VERSION
	{
		version - 0.0.4.0
		change = Alpha 0.4 Kerbal Stuff
		change = I will add a few beauty shots of A0.4 as I take them, lol..
		change = Updated to KSP 1.0.5
		change = Fixed an issue with the bottom attachment node
		change = Split the heat shield off to be a separate object to solve the issue with stock reentry heating
		change = Performance is slightly better than the stock Mk 1-2 pod and heat shield)
		change = Added a texture and normal map for the outer shell of the pod
		change = Added textures for both the exterior and interior windows
		change = Remodeled most of the airlock - added handles for kerbals on EVA
		change = Added handle bars to the interior
		change = Included a remodeled instrument panel
		change = Rearranged gauges for the new panel and for future props (still a WIP)
		change = Various other tweaks that I probably forgot 
	}
}

// CC BY-NC-SA-4.0
// zer0Kerbal